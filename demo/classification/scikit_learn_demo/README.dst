Scikit-Learn API
---------------------------

(Note that, the Scikit-Learn API will not be maintainned anymore.)

Nw, xLearn can support Scikit-Learn style API for users. This demo shows how to 
use Scikit-Learn API for LR, FM and FFM to solve classification problems.